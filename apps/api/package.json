{
  "name": "@goldshore/api-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/resolve-wrangler-config.mjs && wrangler deploy --dry-run --config .wrangler.resolved.toml",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.23.0",
    "miniflare": "^3.20240904.0",
    "typescript": "^5.6.2",
    "vitest": "^1.6.0"
  }
}
